# Keel
The coordination plane for AI coding agents. Run Claude Code, Cursor, Codex, and OpenClaw on the same repo — safely. Region locks, plan reconciliation, a deterministic merge queue, and per-agent cost caps. Plugs into any agent via MCP. Git was for humans; Keel is for agents.
